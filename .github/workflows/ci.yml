name: CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build-linux:
    runs-on: ubuntu-20.04
    steps:
    - uses: actions/checkout@v2
    - name: example build
      run: ./example/build.sh
    - name: tests
      run: ./tests/build.sh
    - name: coverage
      uses: codecov/codecov-action@v3
      with:
        gcov: true
